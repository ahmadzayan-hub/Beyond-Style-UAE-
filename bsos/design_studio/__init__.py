"""AI Custom Design Studio — استوديو التصميم المخصص بالذكاء الاصطناعي.

Turns a customer idea into verified typographic artwork and workshop files.
The load-bearing principle: Arabic spelling is NEVER trusted to an image
model. Text is shaped deterministically (HarfBuzz + the font's own GSUB
rules), verified glyph-by-glyph against the expected letter sequence, and
only then composed into jewellery artwork. AI imagery is confined to the
CONCEPT stage and can never acquire a production status.
"""
